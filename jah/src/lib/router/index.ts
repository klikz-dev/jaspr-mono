import {
    NativeRouter as Router,
    Route,
    RouteProps,
    Link,
    Redirect,
    useLocation,
    BackButton,
    useHistory,
    useRouteMatch,
} from 'react-router-native';

export { Router, Route, Link, Redirect, useLocation, BackButton, useHistory, useRouteMatch };

export type { RouteProps };
