import {
    BrowserRouter as Router,
    Route,
    Link,
    Redirect,
    useHistory,
    useLocation,
    useRouteMatch,
    NavLink,
    Switch,
    RouteProps,
} from 'react-router-dom';

export { Router, Route, NavLink, Switch, Link, Redirect, useHistory, useLocation, useRouteMatch };

export type { RouteProps };
